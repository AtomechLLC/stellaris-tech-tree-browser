---
quick_id: 260708-qt8
title: Expose tech potential gates in the pipeline; wire app to snapshot gates
status: complete
---

# Quick Task 260708-qt8

**Goal:** Ship tech `potential` gates in `tech.json` (pipeline) and switch the Saved Empire feature
to read them from the snapshot, removing the gitignored `tech-gates.json` side-load so a clean build
works. Logic proven in spikes 002 (gates) + 005 (app wiring).

## Task 1 — Pipeline: emit `gate` in tech.json

- **files:** `pipeline/src/gates.ts` (new), `pipeline/src/schema/tech-snapshot.ts`,
  `pipeline/src/assemble.ts`
- **action:**
  1. New `pipeline/src/gates.ts` — the NORMALIZE half of the spike's gate module: `GateNode` type,
     `isStaticTrigger`, `normalizePotential(raw)`. No evaluator (that stays app-side).
  2. `TechSchema`: add `gate` — a recursive `GateNodeSchema` (leaf | and/or/nor/not), `.nullable().default(null)`.
  3. `assemble.ts`: `const gate = normalizePotential(potentialRaw)` (potentialRaw already destructured
     for classifyDlc) → include `gate` in the written Tech.
- **verify:** `npm --prefix pipeline run build:data` succeeds; `tech.json` has `"gate"` on ~376 techs,
  `null` on the rest; `npm --prefix pipeline test` passes.
- **done:** tech.json ships gates; pipeline schema validates them.

## Task 2 — App: consume snapshot gates, drop the side-load

- **files:** `app/src/lib/empire/classifyEmpire.ts`, `app/src/components/EmpirePanel.tsx`
- **action:**
  1. `classifyEmpire.ts`: drop `fetchGates`/`GateMap`; `buildTechLite(snapshot)` reads `t.gate` from
     each tech directly. (App `Tech` type is re-exported from the pipeline schema — `gate` is already
     present, no app-type edit.)
  2. `EmpirePanel.tsx`: remove the gate-fetch state/effect; build `techLite` from the snapshot; classify
     as soon as a save loads.
  3. Remove the side-loaded `app/public/data/v4.5.0/tech-gates.json` usage (file is gitignored).
- **verify:** `npm --prefix app run build` (tsc + vite) passes; Saved Empire tab colors Nocturne Drift
  140/229/291/18, integrity ok, reading gates from tech.json (dev server on :5180).
- **done:** clean build has no side-load; coloring works from tech.json.

## must_haves
- `tech.json` contains a `gate` field on gated techs (truth)
- app build passes and the tab recolors from snapshot gates (artifact)
- no runtime fetch of `tech-gates.json` remains (truth)
