---
phase: 01-data-pipeline
plan: 05
type: execute
wave: 3
depends_on: [01-02, 01-03, 01-04]
files_modified:
  - pipeline/src/assemble.ts
  - pipeline/src/report.ts
  - pipeline/SCHEMA.md
  - pipeline/test/corpus.test.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05]
user_setup: []

must_haves:
  truths:
    - "One command (npm run build:data) produces the complete data/v4.5.0/tech.json for all ~678 techs plus data/v4.5.0/icons/*.webp, with no manual steps"
    - "Every tech record has resolved tier/area/category/cost/weight/prerequisites/unlocks/dlc/flags/name and an icon reference"
    - "Re-running the pipeline produces a byte-identical tech.json (idempotent — deterministic key ordering, D-03/DATA-05)"
    - "The pipeline prints a validation report (counts per area/tier, DLC breakdown, unresolved refs, missing icons/localisation) and strict-fails on structural errors"
    - "SCHEMA.md documents the tech.json contract so Phase 2 can develop against fixture data"
  artifacts:
    - path: "pipeline/src/assemble.ts"
      provides: "full orchestrator composing extractor + DLC + graph + localisation + icons into the validated snapshot"
      min_lines: 60
    - path: "pipeline/src/report.ts"
      provides: "end-of-run validation report (D-17)"
      exports: ["buildReport", "printReport"]
    - path: "pipeline/SCHEMA.md"
      provides: "documented tech.json contract for Phase 2 (D-04)"
    - path: "data/v4.5.0/tech.json"
      provides: "the full versioned snapshot (the phase deliverable)"
    - path: "data/v4.5.0/icons"
      provides: "web-ready per-tech WebP icons"
  key_links:
    - from: "pipeline/src/assemble.ts"
      to: "pipeline/src/parser/tech-extractor.ts"
      via: "extractAllTechs over all 33 files"
      pattern: "extractAllTechs"
    - from: "pipeline/src/assemble.ts"
      to: "pipeline/src/localisation/loc-scanner.ts"
      via: "resolveTechText fills name/description"
      pattern: "resolveTechText"
    - from: "pipeline/src/assemble.ts"
      to: "pipeline/src/icons/convert.ts"
      via: "convertDdsToWebp writes data/v{version}/icons/*.webp"
      pattern: "convertDdsToWebp"
    - from: "pipeline/src/assemble.ts"
      to: "pipeline/src/graph/build-dag.ts"
      via: "buildAndValidateGraph supplies unlocks + validates DAG"
      pattern: "buildAndValidateGraph"
---

<objective>
Refine the walking skeleton's single-tech output into the complete, full-fidelity snapshot: rewrite assemble.ts to compose the full extractor (all 33 files), localisation resolver, DLC classifier, prerequisite graph, and icon converter into a single validated `data/v4.5.0/tech.json` plus `data/v4.5.0/icons/*.webp`, emit the D-17 validation report, guarantee idempotency (D-03/DATA-05), and document the schema contract for Phase 2 (D-04). This is the slice that completes the phase — the skeleton already proved the flow end-to-end; this widens the same flow to every tech and every field.

Purpose: This is the phase's payoff and its most important non-correctness property (D-14/DATA-05): the single command that regenerates everything from scratch, the countermeasure to the reference tool's fatal manual pipeline. It also enforces the full-corpus validation gate (D-18) and the strict-fail policy (D-16) that keep the data trustworthy.

Output: The complete `data/v4.5.0/tech.json`, `data/v4.5.0/icons/*.webp`, a console validation report, and `pipeline/SCHEMA.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-pipeline/01-CONTEXT.md
@.planning/phases/01-data-pipeline/01-RESEARCH.md
@.planning/phases/01-data-pipeline/01-01-SUMMARY.md
@.planning/phases/01-data-pipeline/01-02-SUMMARY.md
@.planning/phases/01-data-pipeline/01-03-SUMMARY.md
@.planning/phases/01-data-pipeline/01-04-SUMMARY.md

<interfaces>
<!-- Every contract this plan composes. Use directly; do not re-explore. -->

From pipeline/src/config.ts: `resolveConfig(): { gameRoot }`
From pipeline/src/version/detect.ts: `detectGameVersion(gameRoot): string` -> "v4.5.0"
From pipeline/src/parser/scripted-variables.ts: `loadScriptedVariables(gameRoot)`, `resolveValue`
From pipeline/src/parser/tech-extractor.ts: `extractAllTechs(gameRoot, varMap): Promise<Tech[]>` (with per-tech source filename)
From pipeline/src/dlc/dlc-registry.ts: `loadDlcRegistry(gameRoot)`
From pipeline/src/dlc/dlc-classifier.ts: `classifyDlc(tech, sourceFilename, dlcRegistry): string|null`
From pipeline/src/graph/build-dag.ts: `buildAndValidateGraph(techs)` -> reverse edges (unlocks) + DAG validation (throws on dangling/cycle)
From pipeline/src/localisation/loc-scanner.ts: `scanAllLocalisation(locDir)`, `resolveTechText(techKey, map): { name, description }`
From pipeline/src/icons/resolve.ts: `resolveIconSource(tech, gameRoot)`
From pipeline/src/icons/convert.ts: `convertDdsToWebp(ddsPath, pngTempPath, webpOutPath)` + placeholder helper
From pipeline/src/schema/tech-snapshot.ts: `TechSnapshotSchema`, `TechSnapshot`, `Tech`

Verified assembly requirements:
- Full corpus: ~678 techs across 33 files (D-18 — validate against the FULL corpus, not a sample).
- Deterministic output (D-03/DATA-05): sort techs by key, sort all edge/array lists, use stable JSON.stringify key ordering and fixed indentation so two runs produce a byte-identical tech.json.
- Strict-fail (D-16): unparseable file, unresolved prerequisite ID, unresolved scripted variable in a required field, or missing tech NAME -> throw, do not write. Warn-and-report: missing icon (placeholder), missing description.
- Report (D-17): counts per area/tier, DLC breakdown, count of unresolved refs, count of missing icons/localisation — printed at the end and cross-checked against the two known cross-DLC host_has_dlc cases (RESEARCH.md Validation Architecture).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validation report module + schema documentation</name>
  <read_first>
    - .planning/phases/01-data-pipeline/01-RESEARCH.md (Validation Architecture section — the suggested report contents)
    - .planning/phases/01-data-pipeline/01-CONTEXT.md (D-03, D-04, D-17 decisions)
    - pipeline/src/schema/tech-snapshot.ts (the TechSnapshot shape to document)
  </read_first>
  <files>pipeline/src/report.ts, pipeline/SCHEMA.md</files>
  <action>
    Create `pipeline/src/report.ts` exporting `buildReport(snapshot, warnings): Report` and `printReport(report): void`. The report (D-17) MUST include: total tech count parsed vs. total tech_* keys found (should match — catches technology_swap leakage per Pitfall 5); count of unresolved @scripted_variable references remaining (must be 0); count of dangling prerequisite references (must be 0); count of techs missing localisation name and missing icon; tech counts per area and per tier; and a DLC breakdown count. Cross-check the DLC breakdown notes the two known cross-DLC host_has_dlc cases. printReport writes a readable summary to console.

    Create `pipeline/SCHEMA.md` documenting the `tech.json` contract (D-04) so Phase 2 can build against fixture data before/independently of pipeline runs: describe the `meta` block fields (gameVersion, generatedAt, techCount, areaCounts, tierCounts, sourceFiles), the `techs` record shape, and every `Tech` field (key, area, category[], tier, cost, costRaw?, weight, weightModifierRaw?, prerequisites[], unlocks, dlc, flags{isRare,isDangerous,isRepeatable,isStarting}, name, description, icon) with types and a note on which are nullable. State explicitly that this is the hard pipeline/frontend boundary and reference the Zod schema module as the source of truth.
  </action>
  <verify>
    <automated>cd pipeline && npx tsc --noEmit && test -f SCHEMA.md && grep -qi "meta" SCHEMA.md && grep -qi "prerequisites" SCHEMA.md && grep -qi "unlocks" SCHEMA.md && echo REPORT_DOC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `report.ts` exports `buildReport` and `printReport`; the report includes area counts, tier counts, DLC breakdown, unresolved-reference count, dangling-prerequisite count, and missing-icon/localisation counts.
    - `SCHEMA.md` exists and documents the meta block, the techs record, and every Tech field including which are nullable, and names the Zod module as the source of truth.
    - `npx tsc --noEmit` passes (report.ts type-checks against the TechSnapshot shape).
  </acceptance_criteria>
  <done>The validation-report module produces the D-17 report contents, and SCHEMA.md documents the full tech.json contract for Phase 2.</done>
</task>

<task type="auto">
  <name>Task 2: Full assembler — compose all stages into the validated snapshot</name>
  <read_first>
    - .planning/phases/01-data-pipeline/01-CONTEXT.md (D-01, D-03, D-05, D-14, D-16, D-17, D-18 decisions)
    - .planning/phases/01-data-pipeline/01-RESEARCH.md (System Architecture Diagram, Validation Architecture)
    - pipeline/src/assemble.ts (the walking-skeleton version from Plan 01 — this task replaces its narrow slice)
    - pipeline/src/parser/tech-extractor.ts, pipeline/src/dlc/dlc-classifier.ts, pipeline/src/dlc/dlc-registry.ts, pipeline/src/graph/build-dag.ts (Plan 02 exports)
    - pipeline/src/localisation/loc-scanner.ts (Plan 03 exports)
    - pipeline/src/icons/resolve.ts, pipeline/src/icons/convert.ts (Plan 04 exports)
    - pipeline/src/report.ts (Task 1 of this plan)
    - pipeline/src/schema/tech-snapshot.ts (TechSnapshotSchema)
  </read_first>
  <files>pipeline/src/assemble.ts</files>
  <action>
    Rewrite `pipeline/src/assemble.ts` to compose the full pipeline (replacing the Plan 01 skeleton's single-tech slice). Order (per the RESEARCH.md System Architecture Diagram): (1) resolveConfig() -> gameRoot; (2) detectGameVersion(gameRoot); (3) loadScriptedVariables(gameRoot); (4) loadDlcRegistry(gameRoot); (5) scanAllLocalisation(<gameRoot>/localisation/english); (6) extractAllTechs(gameRoot, varMap) over all 33 files; (7) for each tech: classifyDlc (dlc), resolveTechText (name/description — STRICT-FAIL if name missing per D-16, warn if description missing), resolveIconSource + convertDdsToWebp writing to data/v{version}/icons/<tech_key>.webp (or the placeholder + warning if no source, D-13), and set tech.icon to the emitted filename; (8) buildAndValidateGraph(techs) -> attach reverse-edge unlocks to each tech, and this call THROWS on any dangling prerequisite or cycle (D-16); (9) sort techs by key and sort every internal list for determinism (D-03); (10) assemble the TechSnapshot with the full meta block (gameVersion, generatedAt, techCount, areaCounts, tierCounts, sourceFiles = the 33 filenames); (11) `TechSnapshotSchema.parse()` the whole snapshot BEFORE writing — throw and write nothing if invalid (D-16); (12) write data/v{version}/tech.json with fixed 2-space indentation and deterministic key ordering; (13) buildReport + printReport (D-17). The entire flow is the single `npm run build:data` command with no manual steps (D-14/DATA-05). Note: `generatedAt` is a timestamp and will differ between runs — to preserve idempotency of the DATA fields (DATA-05), the idempotency test compares tech.json with the meta.generatedAt field normalized/excluded; keep all OTHER content byte-stable.
  </action>
  <verify>
    <automated>cd pipeline && npm run build:data 2>&1 | tail -30 && node --input-type=module -e "import('node:fs').then(fs=>{const s=JSON.parse(fs.readFileSync('data/v4.5.0/tech.json','utf8'));const n=Object.keys(s.techs).length;if(n<650){console.error('too few techs: '+n);process.exit(1);}const namedMissing=Object.values(s.techs).filter(t=>!t.name).length;if(namedMissing>0){console.error('techs missing name: '+namedMissing);process.exit(1);}const withIcon=Object.values(s.techs).filter(t=>t.icon).length;if(withIcon<n){console.error('techs missing icon ref: '+(n-withIcon));process.exit(1);}console.log('FULL_SNAPSHOT_OK techs='+n);})"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build:data` completes with no manual steps and writes data/v4.5.0/tech.json plus data/v4.5.0/icons/*.webp.
    - The snapshot contains 650+ techs (RESEARCH.md counted 678), every tech has a non-empty `name` (strict-fail proved names resolve), and every tech has a non-null `icon` reference (real or placeholder).
    - assemble.ts calls extractAllTechs, classifyDlc, resolveTechText, resolveIconSource/convertDdsToWebp, buildAndValidateGraph, and TechSnapshotSchema.parse() (grep confirms each is wired).
    - The meta block has gameVersion="v4.5.0", techCount matching the techs count, and sourceFiles listing the 33 files.
    - The build fails loud (non-zero exit, no partial write) if a prerequisite is dangling, a required @variable is unresolved, or a tech name is missing (D-16).
  </acceptance_criteria>
  <done>A single command composes all stages into a schema-valid full snapshot of ~678 techs with names, icons, DLC tags, resolved costs/weights, and computed unlocks — writing tech.json and per-tech WebP icons, and printing the validation report.</done>
</task>

<task type="auto">
  <name>Task 3: Full-corpus idempotency + coverage verification</name>
  <read_first>
    - .planning/phases/01-data-pipeline/01-CONTEXT.md (D-03, D-18 decisions; DATA-05 idempotency)
    - .planning/phases/01-data-pipeline/01-RESEARCH.md (Validation Architecture, D-18 full-corpus mandate)
    - pipeline/src/assemble.ts (the full assembler from Task 2)
  </read_first>
  <files>pipeline/test/corpus.test.ts</files>
  <behavior>
    - Test 1 (full-corpus coverage, D-18): after a build, tech.json has 650+ techs and 0 unresolved @variable references and 0 dangling prerequisites (parse the report or the snapshot to assert).
    - Test 2 (idempotency, DATA-05/D-03): running the build twice produces tech.json files that are byte-identical after normalizing the volatile meta.generatedAt field — proving deterministic ordering.
    - Test 3 (localisation coverage, DATA-03): every tech in the snapshot has a non-empty name.
    - Test 4 (icon coverage, DATA-04): every tech has a non-null icon reference and the referenced .webp exists under data/v4.5.0/icons/ (real or the shipped placeholder).
  </behavior>
  <action>
    Create `pipeline/test/corpus.test.ts` — the full-corpus integration test mandated by D-18 (validate against the real 33 files, not a sample). It runs the assembler (or reads the already-built snapshot) and asserts the four behaviors above. For idempotency (Test 2): run the build twice, read both tech.json outputs, strip/normalize `meta.generatedAt`, and assert the remaining JSON is byte-identical (this is the DATA-05 "re-running reproduces an equivalent snapshot" proof — the countermeasure to the reference tool's manual-pipeline death). For coverage: assert techCount >= 650, all names present, all icon refs present with the target .webp on disk, and (via the report) zero unresolved references and zero dangling prerequisites.
  </action>
  <verify>
    <automated>cd pipeline && npx vitest run test/corpus.test.ts 2>&1 | tail -25 | grep -Eiq "pass|✓" && echo CORPUS_OK</automated>
  </verify>
  <acceptance_criteria>
    - The corpus test runs against the real 33-file install (not fixtures) per D-18 and passes.
    - Idempotency: two consecutive builds yield byte-identical tech.json after normalizing meta.generatedAt.
    - techCount >= 650; zero unresolved @variable references; zero dangling prerequisites.
    - Every tech has a non-empty name (DATA-03) and a non-null icon reference whose .webp exists on disk (DATA-04).
  </acceptance_criteria>
  <done>The full-corpus test proves DATA-01 through DATA-05 end-to-end against the real install: complete coverage, resolved data, full localisation, per-tech icons, and byte-stable idempotent regeneration from a single command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| all game files -> assembled snapshot | The full corpus flows through composition into the single shipped artifact |
| snapshot -> Phase 2 (deferred) | tech.json is the contract boundary; localisation strings carried as plain text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Corrupt/incomplete snapshot shipping (the reference-tool failure mode) | mitigate | TechSnapshotSchema.parse() gates the write (D-16); strict-fail on unresolved prerequisites, unresolved required @variables, and missing names; full-corpus test (D-18) asserts 650+ techs, zero unresolved refs, zero dangling prerequisites before the artifact is trusted |
| T-05-02 | Repudiation / Reproducibility | Non-deterministic output making a future patch diff unreadable | mitigate | Deterministic key/list ordering + fixed indentation (D-03) yields byte-identical output across runs (generatedAt normalized); idempotency test enforces it (DATA-05) |
| T-05-03 | Tampering / XSS (deferred to Phase 2) | Localisation §color§/$var$ tokens carried into the snapshot | mitigate | Strings carried as PLAIN TEXT (Plan 03); assembler does not convert markup to HTML; SCHEMA.md documents strings as plain text so Phase 2 renders them safely |
| T-05-SC | Tampering | npm installs | mitigate | No new packages; all deps verified [VERIFIED: npm registry] in Plan 01's Package Legitimacy Audit |
</threat_model>

<verification>
- `npm run build:data` produces the complete data/v4.5.0/tech.json + icons/*.webp with no manual steps (DATA-01/DATA-05, D-14).
- Every tech has resolved tier/area/category/cost/weight/prerequisites/unlocks/dlc/flags/name/icon (DATA-02/DATA-03/DATA-04).
- Re-running yields byte-identical output (idempotent, D-03/DATA-05).
- The validation report prints counts, unresolved refs, missing icons/localisation (D-17), and the build strict-fails on structural errors (D-16).
- SCHEMA.md documents the contract (D-04).
</verification>

<success_criteria>
- All five phase success criteria met: single-command full parse (DATA-01), fully-resolved records with @variables (DATA-02), localised names/descriptions (DATA-03), per-tech web-ready icons (DATA-04), idempotent single-command regeneration (DATA-05).
- Full-corpus validation (D-18) and strict-fail policy (D-16) enforced; validation report emitted (D-17); schema documented (D-04).
</success_criteria>

<output>
Create `.planning/phases/01-data-pipeline/01-05-SUMMARY.md` when done.
</output>
