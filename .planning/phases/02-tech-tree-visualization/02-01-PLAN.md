---
phase: 02-tech-tree-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/package.json
  - app/tsconfig.json
  - app/tsconfig.node.json
  - app/vite.config.ts
  - app/index.html
  - app/.gitignore
  - app/scripts/copy-data.mjs
  - app/src/main.tsx
  - app/src/App.tsx
  - app/src/vite-env.d.ts
  - app/src/types/tech-snapshot.ts
  - app/src/lib/data/fetchSnapshot.ts
  - app/src/lib/graph/buildGraph.ts
  - app/src/components/TechTreeCanvas.tsx
  - app/src/test/smoke.test.ts
  - app/vitest.config.ts
autonomous: true
requirements: [TREE-01, TREE-03]
user_setup: []

must_haves:
  truths:
    - "Running `npm run dev` in app/ serves a page that renders all 678 tech nodes on a Sigma WebGL canvas (new app/ Vite package + sigma stack, D-01; pure static SPA, no server/router, D-02)"
    - "User can pan and zoom the canvas with the mouse (native Sigma WebGL camera, no re-layout/re-render per frame, D-10)"
    - "A failing-then-passing smoke test asserts the built graphology graph has exactly 678 nodes"
    - "tech.json + icons are copied from pipeline/data/v4.5.0/ into app/public/data/v4.5.0/ by an npm script before build/dev"
    - "A fetch/parse failure surfaces as an error, not a blank white screen"
  artifacts:
    - path: "app/package.json"
      provides: "Frontend package with locked deps + copy-data + dev/build/test scripts"
      contains: "sigma"
    - path: "app/scripts/copy-data.mjs"
      provides: "Build-time copy of pipeline data into app/public (D-03)"
      min_lines: 10
    - path: "app/src/lib/data/fetchSnapshot.ts"
      provides: "Fetch + shape-validate tech.json, throwing on failure"
      min_lines: 10
    - path: "app/src/lib/graph/buildGraph.ts"
      provides: "TechSnapshot -> graphology DirectedGraph with all 678 nodes"
      exports: ["buildGraph"]
    - path: "app/src/components/TechTreeCanvas.tsx"
      provides: "SigmaContainer rendering the graph via image nodes"
      min_lines: 20
    - path: "app/src/types/tech-snapshot.ts"
      provides: "Re-export of Phase 1 Tech/TechSnapshot types (D-04 single source of truth)"
      contains: "TechSnapshot"
    - path: "app/src/test/smoke.test.ts"
      provides: "Full-scale smoke test asserting 678 nodes"
      contains: "678"
  key_links:
    - from: "app/src/lib/data/fetchSnapshot.ts"
      to: "/data/v4.5.0/tech.json"
      via: "fetch"
      pattern: "fetch\\("
    - from: "app/src/components/TechTreeCanvas.tsx"
      to: "app/src/lib/graph/buildGraph.ts"
      via: "import + useLoadGraph"
      pattern: "buildGraph"
    - from: "app/scripts/copy-data.mjs"
      to: "app/public/data"
      via: "fs copy"
      pattern: "public/data"
---

<objective>
Stand up the new `app/` Vite + React 19 + TypeScript package and deliver the first genuinely viewable end-to-end capability: open the app, fetch the real 678-tech `tech.json`, build a graphology graph, and render every tech as an icon node on a Sigma WebGL canvas the user can pan and zoom. Nodes are placed on a simple deterministic grid in this plan (real DAG layout with tier columns + area bands is Plan 02); the point of this slice is a working full-scale render + camera, not the final layout.

Purpose: Establishes the app package, the data-copy boundary (D-03), the type-reuse contract (D-04), the fetch-and-fail-safely path, and proves Sigma renders and pans smoothly at the real 678-node scale (TREE-03 benchmark, D-08 mandate to test against the real graph not a sample).
Output: A runnable `app/` package; `npm run dev` shows all 678 icon nodes with working pan/zoom; a smoke test asserting node count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tech-tree-visualization/02-CONTEXT.md
@.planning/phases/02-tech-tree-visualization/02-RESEARCH.md
@pipeline/SCHEMA.md

<interfaces>
<!-- Phase 1 types to reuse (D-04). Do NOT redeclare these — re-export them. -->

From pipeline/src/schema/tech-snapshot.ts:
```typescript
export type TechSnapshot = z.infer<typeof TechSnapshotSchema>;
export type Tech = z.infer<typeof TechSchema>;
// TechSnapshot = { meta: {...}, techs: Record<string, Tech> }
// Tech = { key, area: "physics"|"society"|"engineering", category: string[],
//          tier: number, cost, weight, prerequisites: string[],
//          unlocks: { grants: string[], leadsTo: string[] },
//          dlc: string|null, flags: {...}, name: string,
//          description: string|null, icon: string|null }
export const TechSnapshotSchema; // Zod schema — available for optional runtime .parse() (D-04 / RESEARCH Open Q1)
```

Real data facts (verified against pipeline/data/v4.5.0/tech.json):
- meta.techCount === 678, Object.keys(techs).length === 678
- meta.gameVersion === "v4.5.0"
- icon field is a bare filename e.g. "tech_adaptive_bureaucracy.webp"; ALL 678 have a non-null icon (0 nulls)
- icons live at pipeline/data/v4.5.0/icons/{key}.webp (803 files incl. swap variants)
- areaCounts: society 312, physics 167, engineering 199
</interfaces>

<data_pipeline_reference>
From RESEARCH.md — the locked stack + patterns for this plan:
- Install list (Code Examples "Installation"): react@19.2.7, react-dom@19.2.7, sigma@3.0.3, graphology@0.26.0, @react-sigma/core@5.0.6, @sigma/node-image@3.0.0, @sigma/node-border@3.0.0, elkjs@0.11.1, zustand@5.0.14 (runtime); vite@8.1.3, @vitejs/plugin-react@6.0.3, typescript@5.9.3, @types/react@19, @types/react-dom@19 (dev). @sigma/node-border and elkjs are installed now but first USED in later plans — install the full list once here to avoid churn.
- RESEARCH "Reusing Phase 1 types (D-04) Option A": relative `import type` re-export; widen app/tsconfig include to reach ../pipeline/src/schema/tech-snapshot.ts; OMIT rootDir from app/tsconfig (let TS infer). Do NOT edit pipeline/tsconfig.json (CONTEXT: do not modify Phase 1).
- RESEARCH "Fetching tech.json and building the graphology graph" (Code Examples) — the buildGraph shape.
- RESEARCH "Wiring SigmaContainer with @react-sigma/core" (Code Examples) — SigmaContainer + useLoadGraph pattern.
- RESEARCH Pitfall 1: NO react-sigma/React 19 peer conflict — install as specified, do not add React overrides. Confirm `npm ls react @react-sigma/core` shows no UNMET PEER DEPENDENCY.
</data_pipeline_reference>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold app/ package, data-copy script, and a failing full-scale smoke test</name>
  <files>app/package.json, app/tsconfig.json, app/tsconfig.node.json, app/vite.config.ts, app/vitest.config.ts, app/index.html, app/.gitignore, app/src/vite-env.d.ts, app/scripts/copy-data.mjs, app/src/types/tech-snapshot.ts, app/src/test/smoke.test.ts</files>
  <read_first>
    - .planning/phases/02-tech-tree-visualization/02-RESEARCH.md (sections: "Standard Stack" Installation block; "Reusing Phase 1 types (D-04) Option A"; "Recommended Project Structure"; Pitfall 1)
    - .planning/phases/02-tech-tree-visualization/02-CONTEXT.md (D-01, D-02, D-03, D-04)
    - pipeline/package.json (mirror the TypeScript 5.9.3 pin and the "type":"module" + vitest discipline)
    - pipeline/tsconfig.json (reference only — do NOT edit it; app tsconfig differs: noEmit, moduleResolution bundler, no rootDir)
    - pipeline/src/schema/tech-snapshot.ts (the types being re-exported)
  </read_first>
  <action>
    Create the `app/` package as a pure static SPA (D-01/D-02). package.json: `"type":"module"`, `"private":true`; dependencies and devDependencies EXACTLY the versions from RESEARCH Installation (pin TypeScript to 5.9.3, not ^5 or latest — matches pipeline and resolves the documented 5.x/6.x drift). Scripts: `"copy-data": "node scripts/copy-data.mjs"`, `"predev": "npm run copy-data"`, `"prebuild": "npm run copy-data"`, `"pretest": "npm run copy-data"`, `"dev": "vite"`, `"build": "tsc --noEmit && vite build"`, `"test": "vitest run"`, `"lint:types": "tsc --noEmit"`.

    vite.config.ts: `@vitejs/plugin-react`; default root/publicDir (`public/`). vitest.config.ts (or vitest field in vite.config): environment `jsdom` if a DOM is needed, else `node` for the pure graph smoke test — the smoke test in Task-scope reads the copied tech.json from disk (Node fs) and calls buildGraph, so `node` environment is sufficient and faster.

    tsconfig.json: `strict: true`, `moduleResolution: "bundler"`, `noEmit: true`, `jsx: "react-jsx"`, `lib` includes DOM + ES2022; OMIT `rootDir` (RESEARCH D-04 Option A — avoids TS6059); `include` MUST list `"src/**/*.ts"`, `"src/**/*.tsx"`, and `"../pipeline/src/schema/tech-snapshot.ts"` so the cross-package type re-export resolves. tsconfig.node.json for vite.config typechecking (standard Vite pattern). Do NOT modify pipeline/tsconfig.json.

    app/src/types/tech-snapshot.ts: a re-export module — `export type { Tech, TechSnapshot } from "../../../pipeline/src/schema/tech-snapshot";` and additionally `export { TechSnapshotSchema } from "../../../pipeline/src/schema/tech-snapshot";` (value re-export so runtime validation is available to Task 2 per D-04 / RESEARCH Open Q1). This is the single source of truth (D-04) — no field is ever redeclared in app/.

    app/scripts/copy-data.mjs (D-03): resolve the version from `pipeline/data/` (read the single `v*` dir, or hardcode `v4.5.0` with a comment that it is derived from the snapshot's meta.gameVersion) and recursively copy `pipeline/data/{version}/tech.json` + `pipeline/data/{version}/icons/` into `app/public/data/{version}/`. Use `node:fs` `cp` with `{recursive:true}`. Idempotent (overwrite). Print the copied file count. app/public/data/ MUST be gitignored (it is generated) — add `public/data/` to app/.gitignore along with `node_modules`, `dist`.

    index.html: single `<div id="root">`, `<script type="module" src="/src/main.tsx">`, title "Stellaris Tech Tree". src/vite-env.d.ts: `/// <reference types="vite/client" />`.

    app/src/test/smoke.test.ts (RED first — this is the tdd failing test): read `app/public/data/v4.5.0/tech.json` from disk (via `node:fs`), import `buildGraph` from `../lib/graph/buildGraph`, assert `graph.order === 678` and `graph.type === "directed"`. buildGraph does NOT exist yet, so this test FAILS to import — that is the intended RED state. Run `pretest` (copy-data) so the fixture exists on disk before the test runs.
  </action>
  <behavior>
    - Test (smoke): buildGraph(snapshot) returns a graphology DirectedGraph with `.order === 678`.
    - Test (smoke): the graph is directed (`.type === "directed"`).
    - RED state now: the test cannot import buildGraph (Task 2 creates it) — it must fail for that reason, proving the test is wired.
  </behavior>
  <verify>
    <automated>cd app && npm install && npm run copy-data && test -f public/data/v4.5.0/tech.json && npm test 2>&1 | grep -Eiq "fail|cannot find|error" && echo "RED-OK: smoke test fails as expected (buildGraph missing)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm install` in app/ completes with no ERESOLVE and `npm ls react @react-sigma/core` shows no UNMET PEER DEPENDENCY (RESEARCH Pitfall 1).
    - `app/public/data/v4.5.0/tech.json` exists after `npm run copy-data`, and `app/public/data/v4.5.0/icons/` contains the copied .webp files.
    - app/src/types/tech-snapshot.ts re-exports `Tech`, `TechSnapshot`, and `TechSnapshotSchema` from the pipeline module (no field redeclared).
    - The smoke test currently FAILS (RED) because buildGraph does not yet exist.
    - `public/data/` and `node_modules`/`dist` are gitignored in app/.gitignore.
  </acceptance_criteria>
  <done>App package installs cleanly, data copies into public/, types re-export from Phase 1, and the smoke test is RED (buildGraph missing) — ready for Task 2 to turn it GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fetch + validate tech.json, build the 678-node graphology graph, render all nodes on a pan/zoomable Sigma canvas</name>
  <files>app/src/lib/data/fetchSnapshot.ts, app/src/lib/graph/buildGraph.ts, app/src/components/TechTreeCanvas.tsx, app/src/App.tsx, app/src/main.tsx</files>
  <read_first>
    - app/src/test/smoke.test.ts (the RED test this task must turn GREEN — buildGraph must produce order===678)
    - app/src/types/tech-snapshot.ts (the re-exported Tech/TechSnapshot/TechSnapshotSchema)
    - .planning/phases/02-tech-tree-visualization/02-RESEARCH.md (Code Examples: "Fetching tech.json and building the graphology graph"; "Wiring SigmaContainer with @react-sigma/core"; Pattern 3 note that elkjs is NOT used yet this plan)
    - .planning/phases/02-tech-tree-visualization/02-CONTEXT.md (D-03 fetch at startup, D-04 type reuse, D-05 plain-text security, D-07 all prerequisites are real edges — but edges are deferred to Plan 02; nodes only here)
    - pipeline/SCHEMA.md ("Plain-text string contract" + "Top-level shape" + "Tech fields": icon is a bare filename served from data/v{version}/icons/)
  </read_first>
  <action>
    fetchSnapshot.ts (D-03): `async function fetchSnapshot(version = "v4.5.0"): Promise<TechSnapshot>` — `fetch(\`/data/${version}/tech.json\`)`; on `!res.ok` throw an Error with the status. Parse JSON, then validate with `TechSnapshotSchema.parse(json)` (D-04 / RESEARCH Open Q1 — cheap, catches a corrupted copy as a clean error the UI can show, rather than a deep TypeError). Return the parsed snapshot. Any throw here is caught by App's error state.

    buildGraph.ts (turns the RED smoke test GREEN): `export function buildGraph(snapshot: TechSnapshot): DirectedGraph` using `graphology`'s `DirectedGraph`. For every tech in `Object.values(snapshot.techs)`: `addNode(tech.key, { label: tech.name, tier: tech.tier, area: tech.area, image: \`/data/v4.5.0/icons/${tech.icon}\`, size: 12, x, y, color: "#F7F8FA" })`. Set `x`/`y` from a DETERMINISTIC placeholder grid ONLY (this plan has no ELK): e.g. x = tier * 300, y = (running index within that tier) * 40 — enough separation that nodes do not fully overlap so pan/zoom is meaningful. Real tier-column + area-band layout is Plan 02; leave a `// PLACEHOLDER GRID — replaced by ELK layout in Plan 02` comment. Do NOT add prerequisite edges yet (Plan 02) — but DO NOT invent a reason to skip them permanently; they are a separate slice. Node `label` is `tech.name` (plain text; never injected as HTML — D-05).

    TechTreeCanvas.tsx (RESEARCH "Wiring SigmaContainer"): a `SigmaContainer` from `@react-sigma/core` filling 100% width/height; an inner `GraphLoader` component using `useLoadGraph()` in an effect to load the built graph; use the built-in image node program (`@sigma/node-image`'s `createNodeImageProgram` registered as `nodeProgramClasses` with `defaultNodeType`) so icons render. (The compound border+image ring is Plan 03 — a plain image node is correct for this slice.) Pan/zoom is Sigma's native camera — no extra wiring needed (TREE-03).

    App.tsx: on mount, call fetchSnapshot; hold `{status: "loading"|"error"|"ready", graph}` in React state (a plain useState is fine; a zustand store is Plan 03's concern). While loading, render a minimal centered "Loading…" text (full styled loading overlay is Plan 03). On fetch/parse throw, render a minimal centered error message with the error text and DO NOT blank-screen (must_have: fail safely). When ready, render `<TechTreeCanvas graph={graph} />`. main.tsx: standard React 19 `createRoot(...).render(<App/>)`, import a minimal global stylesheet later (Plan 03 owns tokens.css) — for now an inline `height:100vh` on the root container is acceptable.

    Import `@react-sigma/core/lib/style.css` (its base stylesheet) in main.tsx so the SigmaContainer has correct sizing.
  </action>
  <behavior>
    - Test (smoke): after Task 2, `buildGraph(snapshot).order === 678` — GREEN.
    - Test (smoke): graph is directed.
    - Observable: `npm run dev` renders 678 icon nodes; mouse wheel zooms, drag pans, no re-layout on interaction.
    - Observable: forcing a bad fetch (e.g. rename the copied tech.json) shows the error text, not a blank page.
  </behavior>
  <verify>
    <automated>cd app && npm run copy-data && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passes: smoke test asserts graph.order === 678 and directed (GREEN).
    - `npm run build` exits 0 (`tsc --noEmit` clean + `vite build` succeeds) — full 678-tech data present.
    - fetchSnapshot throws (not returns undefined) on `!res.ok` and on a Zod parse failure.
    - buildGraph produces 678 nodes, each with `image` pointing at `/data/v4.5.0/icons/{icon}` and a plain-text `label`; NO `dangerouslySetInnerHTML` anywhere (D-05).
    - App renders a loading state and an error state; never a blank screen on fetch failure.
    - (Manual/observable, recorded in SUMMARY) `npm run dev` shows all 678 nodes and pan/zoom works smoothly at full scale (TREE-03 first benchmark — note observed responsiveness).
  </acceptance_criteria>
  <done>The app fetches the real 678-tech snapshot, builds a 678-node directed graph, renders every icon node on a Sigma canvas the user can pan/zoom smoothly, and fails safely on a data error. Smoke test GREEN, build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static host → browser fetch | `tech.json` + icon URLs are fetched at runtime from same-origin `public/data/`; content is build-time-controlled, but strings inside (name/description/grants) originate from Paradox game files and are treated as untrusted display text per SCHEMA.md. |
| npm registry → build | 9 runtime + 5 dev packages installed; supply-chain surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering/Elevation (XSS) | Node `label` = tech.name rendered by Sigma; any React text of name/description | mitigate | Render all game strings as plain text only. Sigma's label renderer draws text on canvas (no DOM injection); React chrome uses text nodes (auto-escaped). NEVER `dangerouslySetInnerHTML`. (D-05, SCHEMA.md plain-text contract.) |
| T-02-02 | Denial of Service (blank-screen) | fetch/parse of tech.json | mitigate | fetchSnapshot throws on `!res.ok` and on `TechSnapshotSchema.parse()` failure; App catches and renders an error message, never a blank page. |
| T-02-SC | Tampering | npm installs (react, sigma, graphology, @react-sigma/core, @sigma/node-image, @sigma/node-border, elkjs, zustand, vite, @vitejs/plugin-react, typescript, @types/*) | mitigate | All packages appear in RESEARCH.md "Package Legitimacy Audit" (all 9 runtime packages OK, no postinstall scripts). No [ASSUMED]/[SUS]/[SLOP] packages — no blocking human-verify gate required. Install exact versions from RESEARCH Installation; do not add unaudited packages. |
</threat_model>

<verification>
- `cd app && npm install` — no ERESOLVE, no UNMET PEER DEPENDENCY (`npm ls react @react-sigma/core`).
- `cd app && npm run copy-data && test -f public/data/v4.5.0/tech.json` — data boundary works.
- `cd app && npm test` — smoke test passes: graph.order === 678, directed.
- `cd app && npm run build` — exits 0 (tsc --noEmit clean + vite build).
- Grep gate: `grep -rn "dangerouslySetInnerHTML" app/src` returns nothing (D-05 XSS mitigation).
- Observable: `npm run dev`, all 678 nodes visible, pan/zoom smooth (record responsiveness in SUMMARY per TREE-03).
</verification>

<success_criteria>
- app/ package exists, installs cleanly, and runs a static SPA with no server/router (D-01/D-02).
- The real 678-tech tech.json is fetched at startup and every tech renders as an icon node.
- User can pan and zoom the full 678-node graph smoothly (TREE-03 first benchmark).
- Type contract reused from Phase 1 (D-04) — schema drift would be a compile error.
- Fetch/parse failures surface as an error state, not a blank screen.
</success_criteria>

<output>
Create `.planning/phases/02-tech-tree-visualization/02-01-SUMMARY.md` when done. Record: the observed pan/zoom responsiveness at 678 nodes (TREE-03 benchmark), the final installed versions (confirm no peer conflict), and any deviation from the RESEARCH Installation list.
</output>
