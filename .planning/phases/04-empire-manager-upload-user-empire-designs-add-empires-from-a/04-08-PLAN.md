---
phase: 04-empire-manager-upload-user-empire-designs-add-empires-from-a
plan: 08
type: execute
wave: 5
depends_on: ["04-07"]
files_modified:
  - app/src/test/realFileRoundtrip.test.ts
autonomous: false
requirements: [D-02, D-04, D-05, D-07, D-08]

must_haves:
  truths:
    - "The round-trip guarantee holds against the user's real 400KB designs file, not just fixtures"
    - "A removal produces an archive entry byte-identical to the entry that left the designs file"
    - "An empire added from a real save produces an entry the game itself accepts"
    - "An in-place write leaves a usable backup on disk before the original is overwritten"
  artifacts:
    - path: "app/src/test/realFileRoundtrip.test.ts"
      provides: "Local integration test over the real designs file and sample save, auto-skipped when absent"
      contains: "describe"
  key_links:
    - from: "app/src/test/realFileRoundtrip.test.ts"
      to: "app/src/lib/empire/designsText.ts"
      via: "parseDesignsFile + spliceDesignsFile over real bytes"
      pattern: "spliceDesignsFile"
    - from: "app/src/test/realFileRoundtrip.test.ts"
      to: "app/src/lib/empire/designFromSav.ts"
      via: "extractDesignFromCountry over the real sample save"
      pattern: "extractDesignFromCountry"
---

<objective>
Prove the phase's safety claims against real data and a real game client: an integration test over the
user's actual 400KB designs file and the repo's sample save, then human verification of the browser flow,
the File System Access write-back with its backup, and the ultimate acceptance test — Stellaris loading the
file the app produced.

Purpose: every prior plan verified itself against fixtures. This plan verifies against reality, which is
the only place the "don't corrupt the user's designs" promise actually matters.
Output: one local integration test plus signed-off human verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-empire-manager-upload-user-empire-designs-add-empires-from-a/04-CONTEXT.md
@.planning/phases/04-empire-manager-upload-user-empire-designs-add-empires-from-a/04-UI-SPEC.md

<interfaces>
<!-- Real-data facts measured during planning; the integration test encodes them as expectations. -->
- Real designs file: `C:\Users\alexy\Documents\Paradox Interactive\Stellaris\user_empire_designs_v3.4.txt`,
  399,931 bytes, CRLF, 183 top-level entry spans, 182 distinct jomini top-level keys (the name
  "Cyrrician Core" appears twice), 7 entry names containing raw 0x11 colour-escape bytes, valid UTF-8
  whose decode→encode round-trip is byte-identical.
- Sample save: `app/public/data/v4.5.0/sample.sav` (gitignored local file, 6.25 MB), 133 countries,
  325 `species_db` entries, 2081 leaders of which 29 carry a `design` snapshot.
- `app/vitest.config.ts` uses `environment: "node"`, so `node:fs` is available in tests.
- `app/package.json` scripts: `test` (`vitest run`, with a `pretest` copy-data step), `build`
  (`tsc --noEmit && vite build`), `dev` (`vite`, with a `predev` copy-data step).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real-data integration test (auto-skipped in a clean checkout)</name>
  <files>app/src/test/realFileRoundtrip.test.ts</files>
  <read_first>
    - app/src/lib/empire/designsText.ts (parseDesignsFile / spliceDesignsFile / encodeDesignsText)
    - app/src/lib/empire/designFromSav.ts (extractDesignFromCountry signature)
    - app/src/lib/empire/designSerialize.ts (serializeDesignEntry signature)
    - app/src/lib/empire/savLoad.ts (loadEmpiresFromSav and the SavedEmpire.design field)
    - app/src/test/smoke.test.ts (the `node:fs` + `fileURLToPath` disk-reading test idiom already used in this package)
  </read_first>
  <behavior>
    - The whole suite is skipped (not failed) when neither input file is present, so a clean checkout and CI
      stay green: gate with `existsSync` on both paths and use vitest's conditional `describe.skipIf`.
    - Designs path resolution: `process.env.STELLARIS_DESIGNS_FILE` when set, else the known local path
      `C:\Users\alexy\Documents\Paradox Interactive\Stellaris\user_empire_designs_v3.4.txt`.
    - Parse: `parseDesignsFile` over the real bytes yields 183 entries and does not throw, proving the
      array-aware reconciliation tolerates the duplicate "Cyrrician Core" name.
    - Zero-change round trip: `spliceDesignsFile(file, new Set(), [])` strictly equals the decoded text, and
      `encodeDesignsText` of that result is byte-identical to the bytes read from disk.
    - Removal: removing a chosen index yields a designs output that does not contain the removed entry's text
      and still parses to 182 entries; the archive output built from that same index contains the removed
      entry's text as an exact substring.
    - Colour-escape names survive: at least one parsed entry's `rawName` contains `String.fromCharCode(0x11)`,
      and that entry's bytes are unchanged in a zero-change round trip.
    - Add-from-save: loading `app/public/data/v4.5.0/sample.sav` through `loadEmpiresFromSav` yields empires
      whose `design` is non-null; serializing the first such design and appending it via `spliceDesignsFile`
      produces text that `parseDesignsFile` accepts with exactly 184 entries.
  </behavior>
  <action>
    Create `app/src/test/realFileRoundtrip.test.ts` implementing the behavior block. Read both inputs with
    `readFileSync` and wrap in `Uint8Array`. Resolve the sample save relative to the test file using the
    `dirname(fileURLToPath(import.meta.url))` idiom already used in `smoke.test.ts`.

    Give the suite a generous timeout (the save parse is multi-second on a 6 MB save; the designs parse is
    ~25 ms) and a header comment stating that this suite is a LOCAL integration check over gitignored/private
    files, that it self-skips when those files are absent, and that the equivalent guarantees are covered
    deterministically by `designsText.test.ts`, `designSerialize.test.ts`, `designFromSav.test.ts`,
    `designsOutputs.test.ts` and `stageAdd.test.ts`.

    Do not copy, commit, or write any part of the user's real designs file into the repository — read it,
    assert on it, and let it go. Do not write any output file to disk from this test.
  </action>
  <verify>
    <automated>cd app && npx vitest run src/test/realFileRoundtrip.test.ts && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && npx vitest run src/test/realFileRoundtrip.test.ts` exits 0 and, on this machine (where both files exist), reports passing — not skipped — tests.
    - A test asserts the parsed entry count is exactly 183 for the real file.
    - A test asserts byte identity between `encodeDesignsText(spliceDesignsFile(file, new Set(), []))` and the bytes read from disk.
    - A test asserts the post-append parse yields exactly 184 entries.
    - `grep -c 'skipIf' app/src/test/realFileRoundtrip.test.ts` returns at least 1 and `grep -c 'writeFileSync' app/src/test/realFileRoundtrip.test.ts` returns 0.
    - `cd app && npx vitest run` (full suite) exits 0.
  </acceptance_criteria>
  <done>The round-trip, removal, archive and add guarantees are machine-verified against the user's real 400KB file and the real sample save.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser verification — upload, remove, add, download</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/04-empire-manager-upload-user-empire-designs-add-empires-from-a/04-UI-SPEC.md (Copywriting Contract and Save / Download Outcome Flow — the exact strings and sequencing being checked)
  </read_first>
  <action>
    Start the dev server with `cd app && npm run dev`, hand the user the printed localhost URL, and pause for
    the user to walk the script in `how-to-verify` below. Do not screenshot the app — hand over the URL (the
    canvas-heavy tree view stalls automated capture). Do not modify any source file in this task; if the walk
    surfaces a defect, record it and stop for the user's direction rather than fixing it inline.
  </action>
  <what-built>
    An Empire Designs manager reachable from the Saved Empire panel: uploads the designs file, lists all 183
    designs with authority/species metadata and a name filter, stages removals and additions from a loaded
    `.sav`, and on save emits an updated designs file plus an archive of everything removed.
  </what-built>
  <how-to-verify>
    1. Run `cd app && npm run dev` and open the printed localhost URL. Work on a COPY of the designs file, not
       the original: copy `C:\Users\alexy\Documents\Paradox Interactive\Stellaris\user_empire_designs_v3.4.txt`
       to a scratch folder first.
    2. In the Saved Empire panel, confirm "Manage my empire designs" is visible BEFORE loading any `.sav`.
       Click it — the dialog opens centered over a dimmed backdrop.
    3. Drop the copied designs file onto the upload zone. Expect: the zone collapses to a monospace filename
       row with a file-mode badge, and the list shows 183 rows. Type a few letters into "Filter your designs…"
       and confirm the list narrows by name.
    4. Click "Remove" on two designs. Expect: both names go line-through at reduced opacity, the action flips
       to "Undo", the footer count reads "2 pending changes: 0 added, 2 removed", and the summary strip shows
       "Removed designs are archived, not deleted." Click "Undo" on one and confirm the count drops to 1.
    5. Close the dialog with ✕. Expect: the entry-point button now reads "Manage my empire designs · 1 pending".
       Re-open it and confirm the staged removal is still there.
    6. Load a `.sav` (the dev-only "▶ Load sample save" button is fine), select any empire — try an AI empire,
       not just the player — and click "Add to my empires". Expect: the label becomes "Added ✓ — undo". If the
       name already exists, a gold note states the name it will be saved under instead.
    7. Click "Save changes". Expect the confirmation row (not a second modal) stating the added/archived counts
       and that removed designs are never deleted. Click "Save & continue".
    8. Confirm two files download: the designs file under the SAME name you uploaded, and
       `user_empire_designs_archive.txt`. Open both. Expect: the archive contains the removed design(s) verbatim;
       the designs file contains your added empire as a new entry at the end, formatted with tabs like every
       other entry; and a diff against your copy shows ONLY the removed and added entries changed — no
       whitespace or line-ending churn anywhere else.
    9. Confirm the success banner reads "Saved — 1 added, 1 archived." (counts matching what you staged).
  </how-to-verify>
  <verify>
    <human-check>All nine steps above behave as described, and the diff in step 8 shows only the intended entries changed.</human-check>
  </verify>
  <acceptance_criteria>
    - The entry-point button is visible with no `.sav` loaded, and shows the pending count after the dialog is closed with staged changes.
    - The uploaded file lists 183 designs and the filter narrows them.
    - The downloaded designs file keeps the uploaded filename, and diffs against the source show only the intended entries changed (no line-ending or whitespace churn).
    - The archive file contains the removed design(s) byte-for-byte.
    - An AI (non-player) empire can be added, confirming D-06.
    - Every string encountered matches the UI-SPEC Copywriting Contract.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what differed from the expected behaviour above.</resume-signal>
  <done>The full upload → stage → save → download loop is confirmed correct against a real file in a real browser.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: In-place write with backup, and the game-loads-it test</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/04-empire-manager-upload-user-empire-designs-add-empires-from-a/04-RESEARCH.md (Assumptions Log A6 — the manual in-game smoke test this task discharges; Pattern 5 backup sequencing)
  </read_first>
  <action>
    Keep the dev server from Task 2 running, hand the user the URL again, and pause while they walk the
    write-back and in-game script below. This task discharges RESEARCH.md assumption A6 (serializer whitespace
    style has never been checked against the actual game client) — record the outcome explicitly in the
    SUMMARY, including which added-empire fields came through correctly, so the assumption is closed rather
    than carried forward. Change no source files in this task.
  </action>
  <what-built>
    File System Access write-back (Chromium only) that saves changes straight back into the picked file, always
    preceded by an automatic timestamped backup download, with an automatic fallback to plain downloads when
    permission is denied or the API is unavailable.
  </what-built>
  <how-to-verify>
    1. In Chrome or Edge, open the app and the manager dialog. Click the upload zone — a native file picker
       should appear (not a plain file input). Pick your SCRATCH COPY of the designs file, and grant edit
       permission when prompted.
    2. Confirm the file-mode badge reads "In-place saving enabled" with a green dot.
    3. Stage one removal and one addition, click "Save changes", and confirm the confirmation copy now also
       says "A backup of the original file downloads automatically before saving." Click "Save & continue".
    4. Confirm a file named like `<yourfile>.txt.<timestamp>.bak` lands in your downloads folder, and that its
       contents are the ORIGINAL file exactly as it was before the save. Confirm the scratch copy on disk has
       been updated in place, and that the archive still arrived as a download.
    5. Confirm the success banner names the backup file.
    6. Repeat step 3 a second time WITHOUT re-uploading (stage another removal and save again). Confirm the
       second save is also correct — this exercises the post-save re-baseline.
    7. Open the same app in Firefox (or another non-Chromium browser). Confirm the upload zone still works via
       the plain file input, the badge reads "Download only", and saving produces downloads with no error.
    8. FINAL ACCEPTANCE (RESEARCH assumption A6): copy the app-produced designs file over the real
       `user_empire_designs_v3.4.txt` in `Documents\Paradox Interactive\Stellaris\` — keeping your own backup —
       then launch Stellaris, open the empire creation screen, and confirm (a) every pre-existing design is
       still listed and unchanged, and (b) the empire you added from the save appears and is selectable/playable
       with the right species, traits, flag, ruler, ethics, civics and origin.
  </how-to-verify>
  <verify>
    <human-check>The backup file exists and matches the pre-save original, both browser paths behave as described, and Stellaris loads the produced file with all prior designs intact and the added empire playable.</human-check>
  </verify>
  <acceptance_criteria>
    - The picker path is used on Chromium and the badge reflects the write mode correctly on both browser families.
    - A timestamped `.bak` download appears BEFORE the in-place write, and its contents equal the pre-save original.
    - Two consecutive saves in one session both produce correct output.
    - The non-Chromium path produces downloads with no error and no dead-end state.
    - Stellaris loads the produced file with all pre-existing designs intact and the added empire present and playable.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what differed — for the in-game step, note any field on the added empire that came through wrong.</resume-signal>
  <done>The destructive path is proven safe (backup first, correct output, graceful fallback) and the produced file is confirmed game-loadable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification activity → the user's real data | Verification itself must not risk the original file |
| test process → repository | The real designs file is private user data and must never enter the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-28 | Data loss | human verification against the real file | mitigate | Both checkpoints instruct working on a scratch COPY, and the in-game step requires an independent user-held backup before overwriting the original. |
| T-04-29 | Information disclosure | private user data in the repo | mitigate | The integration test reads the real file and asserts on it but never writes it; `writeFileSync` is absent (grep gate) and no fixture is committed. |
| T-04-30 | Repudiation | unverifiable claims about game-loadability | mitigate | RESEARCH assumption A6 is discharged by an explicit in-game load step whose outcome is recorded in the SUMMARY. |
| T-04-SC | Tampering | npm installs | mitigate | No packages installed. Any `npm install` during execution stops for human review. |
</threat_model>

<verification>
- `cd app && npx vitest run` (full suite, including the real-data integration suite) passes on this machine.
- `cd app && npm run build` exits 0.
- Both human checkpoints returned "approved".
</verification>

<success_criteria>
- The round-trip and archive guarantees are verified against the real 400KB file, not only fixtures.
- The in-place write is confirmed to be preceded by a usable backup, with a working non-Chromium fallback.
- Stellaris loads the app-produced designs file with all prior designs intact and the added empire playable.
</success_criteria>

<output>
Create `.planning/phases/04-empire-manager-upload-user-empire-designs-add-empires-from-a/04-08-SUMMARY.md` when done.
</output>
